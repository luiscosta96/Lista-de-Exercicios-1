#include <stdio.h>

int main() {
    system("cls"); //limpa a tela
    const int valor = 10; // Define uma constante do tipo inteiro

    printf("O valor eh = %d\n", valor);

    return 0; 
}